import {Box} from "@mui/system";

const DealsList = () => {
  return (
    <Box>
      <Box></Box>
    </Box>
  );
};

export default DealsList;

export const OpenDeals = () => {};

export const ClosedDeals = () => {};
