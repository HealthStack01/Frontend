import React from 'react';

interface Props {
  color?: string;
  className?: string;
}

const AttendInfoBox: React.FC<Props> = ({ color }) => {
  return <div>AttendInfoBox</div>;
};

export default AttendInfoBox;
